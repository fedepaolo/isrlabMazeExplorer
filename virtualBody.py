from pycsim import CSim, common
import time

class virtualBody:
    def __init__(self,controller)
        percepts=[]
        command=[]
        Controller=controller

    def move_forward(timeout)
            controller.move_forward(timeout)