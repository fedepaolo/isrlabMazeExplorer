from pycsim import CSim, common
import time

class topLayer:
    def __init__(self,plan,virtualBody)
        self.interfaces=vitualBody
        self.plan=None

    def setplan(plan)
        self.plan=plan

    def move_forward()
        timeout=10
        interfaces.move_forward(timeout)
        
